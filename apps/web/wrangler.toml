name = "agent-explorer-web"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Pages configuration
# Note: Environment variables should be configured in Cloudflare Pages dashboard
# Settings → Environment variables

# For local development with wrangler
# [env.development]
# vars = { NODE_ENV = "development" }

# For production
# [env.production]
# vars = { NODE_ENV = "production" }

# Note: pages_build_output_dir should NOT be set when using the Next.js framework preset
# The Next.js preset handles the output directory automatically
# Updated for Cloudflare Pages Git-connected deployment
